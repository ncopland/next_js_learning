/** @type {import('next').NextConfig} */
// Imports the type definition for NextConfig from 'next' package

const nextConfig = {}; 
// Declares a constant 'nextConfig' and initializes it as an empty object

export default nextConfig; 
// Exports 'nextConfig' as the default export of the file
