module.exports = { /* Exports an object from this module that will apply css styling and make tailwind available to be used throughout the project */
  plugins: { /* Defines plugins property */
    tailwindcss: {}, /* Includes TailwindCSS as a plugin for utility first CSS styling throughout the project */
    autoprefixer: {}, /* Includes Autoprefixer as a plugin to automatically add prefixes to CSS rules throughout the project */
  }, /* Closes the plugins object */
}; /* Closes the module.exports object */
