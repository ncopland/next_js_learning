# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules 
# ignores the node_modules directory when uploading to git (where npm packages are installed)
/.pnp
.pnp.js
# ignores .pnp and .pnp.js files when uploading to git
.yarn/install-state.gz
#Ignores yanr's installation state file

# testing
/coverage
# Ignores the coverage directory which is typically generated by code coverage tools

# next.js
/.next/
# Ignores the .next directory which is the output directory for Next.js
/out/
# Ignores the out directory used for output of statis exports when using git

# production
/build
# Ignores the build directory which is used for production builds outputs

# misc
.DS_Store
# does nothing cause I have a windows pc
*.pem
# Ignores files with .pem used for private keys

# debug
npm-debug.log*
# Ignores npm debug log files
yarn-debug.log*
# Ignores yarn debug
yarn-error.log*
# Ignores yarn error files

# local env files
.env*.local
# Ignores local environment files with .local

# vercel
.vercel
# Ignores vercel directory used for project config

# typescript
*.tsbuildinfo
# Ignores TypeScript build info files
next-env.d.ts
# Ignores the next-env.d.ts file which is automatically generated by Next.js