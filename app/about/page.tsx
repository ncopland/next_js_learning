// Creates and exports a functional component 'About' as the default function for the page.tsx file
export default function About() {
    // returns the tsx of the 'About' function starting here
    return (
        // Specifies the main content of the document (I am not sure it does much of anything)
        // h1 just signifies a heading in HTML
        <main>
            <h1>
                About Pymetrics Page
            </h1>
        </main>
        // Closing tags for both the main and head
    )
    // closes the return statement
}
// closes the function 'About'